    var checkExist = setInterval(function() {
       if ($('#popular-product-1').length) {
         $('#popular-product-1').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-1').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-2').length) {
         $('#popular-product-2').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-2').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-3').length) {
         $('#popular-product-3').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-3').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-4').length) {
         $('#popular-product-4').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-4').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-5').length) {
         $('#popular-product-5').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-5').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-6').length) {
         $('#popular-product-6').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-6').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-7').length) {
         $('#popular-product-7').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-7').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-8').length) {
         $('#popular-product-8').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-8').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms






    var checkExist = setInterval(function() {
       if ($('#popular-product-9').length) {
         $('#popular-product-9').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-9').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-10').length) {
         $('#popular-product-10').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-10').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-11').length) {
         $('#popular-product-11').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-11').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-12').length) {
         $('#popular-product-12').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-12').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-13').length) {
         $('#popular-product-13').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-13').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-14').length) {
         $('#popular-product-14').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-14').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-15').length) {
         $('#popular-product-15').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-15').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
        var checkExist = setInterval(function() {
       if ($('#popular-product-16').length) {
         $('#popular-product-16').button().click(function(){
             var getUrl = window.location;
             var baseUrl = getUrl .protocol + "//" + getUrl.host + "/" + getUrl.pathname.split('/')[0];

             var q = $('#popular-product-8').attr('class');
             window.location.href= baseUrl + 'product/' +  q;
         })
          clearInterval(checkExist);
       }
    }, 100); // check every 100ms
